<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
  <title>Crypto Cards</title>
</head>

<body>
  <h1 class="page-title gradient-text">Crypto Cards</h1>

  <div class="cards">

    <% for(let cryptoPair in cryptoData) { %>
      <div class="card">
        <div class="card__property">
          <div class="card__title">
            <%= cryptoData[cryptoPair].base %>
          </div>
        </div>

        <div class="card__property">
          <div class="card__field">Price</div>
          <div class="card__price">
            <%=Math.round(cryptoData[cryptoPair].price * 100) / 100 %>
              <span class="card__currency">
                <%= cryptoData[cryptoPair].target %>
              </span>
          </div>
        </div>

        <div class="card__property">
          <div class="card__field">Volume</div>
          <div class="card__volume">
            <% if (cryptoData[cryptoPair].volume==="" ) { %>
              <span class="card__unavailable"><em>Data not available</em></span>
              <% } else { %>
                <%=Math.round(cryptoData[cryptoPair].volume) %>
                  <% } %>
          </div>
        </div>
      </div>
      <% } %>

  </div>

  <!-- <script src="/index.js"></script> -->
</body>

</html>